{
  "version": 1,
  "max_steps": 30,
  "retries": 0,
  "nodes": {
    "implement": {
      "kind": "agent",
      "provider": "codex",
      "instruction": "Read the GitHub Issue identified by run_input in github_repository using available tools, then implement it completely in the assigned worktree. Follow repository development instructions and run relevant checks. When done, commit your changes in the assigned worktree with a concise, human-readable commit message describing what changed and why, following the repository's commit message conventions and referencing the Issue. Do not push, open pull requests, merge, or comment. Do not reset usage limits, buy allowance, or switch models/providers; stop on a usage limit."
    },
    "publish": {
      "kind": "agent",
      "provider": "codex",
      "instruction": "Create the pull request for the GitHub Issue identified by run_input in github_repository, or update it if inputs[0].data.pr already identifies one, so that its head contains this worktree's changes. Follow the repository's conventions and pull request template for the branch, title and body, and include \"Closes #N\" for the Issue in the body. Do not merge. Return the PR identity. Do not reset usage limits, buy allowance, or switch models/providers; stop on a usage limit.",
      "schema": {
        "type": "object",
        "properties": {
          "pr": {
            "type": "object",
            "description": "The pull request this Run publishes; forward it unchanged unless it changes.",
            "properties": {
              "number": {
                "type": "integer"
              },
              "url": {
                "type": "string"
              },
              "head_sha": {
                "type": "string",
                "description": "Commit SHA currently at the PR head on GitHub."
              }
            },
            "required": [
              "number",
              "url",
              "head_sha"
            ],
            "additionalProperties": false
          }
        },
        "required": [
          "pr"
        ],
        "additionalProperties": false
      }
    },
    "review": {
      "kind": "agent",
      "provider": "codex",
      "instruction": "Review the pull request in inputs[0].data.pr against the GitHub Issue identified by run_input in github_repository. Check both missing scope (partial implementation) and unnecessary scope (changes or complexity the Issue does not justify), correctness, and tests; follow the repository's review guidelines if present and run relevant checks. Set approved only when no blocking findings remain, and list each blocking problem in findings. Leave files unchanged; do not push, merge, or comment. Forward pr unchanged. Do not reset usage limits, buy allowance, or switch models/providers; stop on a usage limit.",
      "schema": {
        "type": "object",
        "properties": {
          "pr": {
            "type": "object",
            "description": "The pull request this Run publishes; forward it unchanged unless it changes.",
            "properties": {
              "number": {
                "type": "integer"
              },
              "url": {
                "type": "string"
              },
              "head_sha": {
                "type": "string",
                "description": "Commit SHA currently at the PR head on GitHub."
              }
            },
            "required": [
              "number",
              "url",
              "head_sha"
            ],
            "additionalProperties": false
          },
          "approved": {
            "type": "boolean",
            "description": "True only when no blocking review findings remain."
          },
          "findings": {
            "type": "array",
            "description": "Blocking problems that must be fixed before approval.",
            "items": {
              "type": "string"
            }
          }
        },
        "required": [
          "pr",
          "approved",
          "findings"
        ],
        "additionalProperties": false
      }
    },
    "fix": {
      "kind": "agent",
      "provider": "codex",
      "instruction": "Fix the findings in inputs[0].data.findings in the assigned worktree and run relevant checks. When done, commit your changes with a concise, human-readable commit message describing what was fixed and why, following the repository's commit message conventions and referencing the Issue. Do not push or merge. Forward inputs[0].data.pr unchanged and summarize the fixes. Do not reset usage limits, buy allowance, or switch models/providers; stop on a usage limit.",
      "schema": {
        "type": "object",
        "properties": {
          "pr": {
            "type": "object",
            "description": "The pull request this Run publishes; forward it unchanged unless it changes.",
            "properties": {
              "number": {
                "type": "integer"
              },
              "url": {
                "type": "string"
              },
              "head_sha": {
                "type": "string",
                "description": "Commit SHA currently at the PR head on GitHub."
              }
            },
            "required": [
              "number",
              "url",
              "head_sha"
            ],
            "additionalProperties": false
          },
          "summary": {
            "type": "string"
          }
        },
        "required": [
          "pr",
          "summary"
        ],
        "additionalProperties": false
      }
    },
    "merge": {
      "kind": "agent",
      "provider": "codex",
      "instruction": "Merge the pull request in inputs[0].data.pr only if its head is still inputs[0].data.pr.head_sha, with a merge commit (not squash or rebase) so the GitWeave checkpoint commits and their notes stay in the branch history, following GitHub policy. Do not bypass required checks or reviews. Report whether it merged and forward pr. Do not reset usage limits, buy allowance, or switch models/providers; stop on a usage limit.",
      "schema": {
        "type": "object",
        "properties": {
          "pr": {
            "type": "object",
            "description": "The pull request this Run publishes; forward it unchanged unless it changes.",
            "properties": {
              "number": {
                "type": "integer"
              },
              "url": {
                "type": "string"
              },
              "head_sha": {
                "type": "string",
                "description": "Commit SHA currently at the PR head on GitHub."
              }
            },
            "required": [
              "number",
              "url",
              "head_sha"
            ],
            "additionalProperties": false
          },
          "merged": {
            "type": "boolean"
          },
          "merge_commit": {
            "type": "string",
            "description": "Merge commit SHA; omit when not merged."
          }
        },
        "required": [
          "pr",
          "merged"
        ],
        "additionalProperties": false
      }
    },
    "close_issue": {
      "kind": "agent",
      "provider": "codex",
      "instruction": "If inputs[0].data.merged is true, make sure the GitHub Issue identified by run_input in github_repository is closed, referencing the merged pull request in inputs[0].data.pr; do nothing if it is already closed. Otherwise leave it open. Report whether it is closed. Do not reset usage limits, buy allowance, or switch models/providers; stop on a usage limit.",
      "schema": {
        "type": "object",
        "properties": {
          "closed": {
            "type": "boolean"
          }
        },
        "required": [
          "closed"
        ],
        "additionalProperties": false
      }
    }
  },
  "flow": [
    "implement",
    "publish",
    "review",
    {
      "loop": {
        "flow": [
          {
            "if": {
              "condition": {
                "path": "/0/data/approved",
                "equals": true
              },
              "then": [],
              "else": [
                "fix",
                "publish",
                "review"
              ]
            }
          }
        ],
        "while": {
          "path": "/0/data/approved",
          "equals": false
        }
      }
    },
    "merge",
    "close_issue"
  ]
}
